version: '3.9'

services:
  postgres:
    image: postgres
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_PASSWORD: 123
      POSTGRES_USER: postgres
    ports:
      - '5432:5432'
    command: -p 5432
